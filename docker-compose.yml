services:
    postgres:
        image: postgres:18-alpine
        container_name: etrs-postgres
        restart: always
        environment:
            POSTGRES_DB: ${POSTGRES_DB}
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        ports:
            - "${POSTGRES_EXTERNAL_PORT:-5437}:5432"
        volumes:
            - postgres_data:/var/lib/postgresql

    pgadmin:
        image: dpage/pgadmin4
        container_name: etrs-pgadmin
        restart: always
        environment:
            PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
            PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
        ports:
            - "${PGADMIN_EXTERNAL_PORT:-5050}:80"
        depends_on:
            - postgres

    db:
        image: mcr.microsoft.com/mssql/server:2022-latest
        container_name: event-db
        ports:
            - "${MSSQL_EXTERNAL_PORT:-1433}:1433"
        environment:
            - ACCEPT_EULA=Y
            - MSSQL_SA_PASSWORD=${MSSQL_SA_PASSWORD}
        volumes:
            - sql-data:/var/opt/mssql

    rabbitmq:
        image: rabbitmq:3-management-alpine
        container_name: etrs-rabbitmq
        restart: always
        environment:
            RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
            RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
        ports:
            - "5672:5672"
            - "15672:15672"

    auth-service:
        build:
            context: ./event-ticket-auth-service
            dockerfile: event-ticket-auth-service/Dockerfile
        environment:
            ASPNETCORE_ENVIRONMENT: Development
            JWT_KEY: ${JWT_KEY}
            JWT_ISSUER: ${JWT_ISSUER}
            JWT_AUDIENCE: ${JWT_AUDIENCE}
        ports:
            - "8080:8080"
            - "8181:8181"

    mock-auth-server:
        image: wiremock/wiremock:3.13.2
        container_name: mock-auth-server
        ports:
            - "5005:8080"
        command: >
            --global-response-templating
        volumes:
            - ./event-ticket-main-services/wiremock/mappings:/home/wiremock/mappings

    core-service:
        build:
            context: ./event-ticket-main-services
            dockerfile: etrs-core/Dockerfile
        container_name: etrs-core
        restart: always
        environment:
            - spring.datasource.url=jdbc:postgresql://postgres:5432/${POSTGRES_DB}
            - spring.datasource.username=${POSTGRES_USER}
            - spring.datasource.password=${POSTGRES_PASSWORD}
            - JWT_KEY=${JWT_KEY}
        ports:
            - "8081:8081"
        depends_on:
            - postgres

    order-service:
        build:
            context: ./event-ticket-main-services
            dockerfile: etrs-order-service/Dockerfile
        container_name: etrs-order-service
        restart: always
        environment:
            - spring.r2dbc.url=r2dbc:postgresql://postgres:5432/${POSTGRES_DB}
            - spring.r2dbc.username=${POSTGRES_USER}
            - spring.r2dbc.password=${POSTGRES_PASSWORD}
            - spring.rabbitmq.host=rabbitmq
            - JWT_KEY=${JWT_KEY}
            - EVENT_SERVICE_URL=${EVENT_SERVICE_URL:-http://core-service:8081}
        ports:
            - "8082:8082"
        depends_on:
            - postgres
            - rabbitmq
            - core-service
volumes:
    postgres_data:
    sql-data:
